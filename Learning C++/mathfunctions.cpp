#include <iostream>
#include <cmath>

int main()
{
    double x = 3;
    double y = 4;
    double z;

    //z = std::max(x,y) //returns the highest value of the 2 values
    //z = std::min(x,y); //return the lowest value of the 2 values
    //z = pow(2, 3) //first number to the power of the 2nd
    //z = sqrt(9) //square roots
    //z = abs(-3); //finds the absolute value (postive version of negative number)
    //z = round(3.14); //rounds value
    //z = ceil(3.14); //rounds a value ALWAYS up
    //z = ceil(3.99); //rounds a value ALWAYS down

    //https://cplusplus.com/reference/cmath/ for all useful math functions

    std::cout << z;

    return 0;
}